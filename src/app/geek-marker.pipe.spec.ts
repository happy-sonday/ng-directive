import { GeekMarkerPipe } from './geek-marker.pipe';

describe('GeekMarkerPipe', () => {
  it('create an instance', () => {
    const pipe = new GeekMarkerPipe();
    expect(pipe).toBeTruthy();
  });
});
